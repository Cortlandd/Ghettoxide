# Ghettoxide

_An android `architecture` based on Dioxide_

# Installation
```kotlin
repositories {
    maven { url = uri("https://jitpack.io") }
}
```

```kotlin
dependencies {
  implementation("com.github.Cortlandd.Ghettoxide:Library:1.0.0")
}
```